events {
    worker_connections 1024;
}

http {
    upstream app {
        server app:3000;
    }

    server {
        listen 80;
        server_name gestaoconsert.com.br www.gestaoconsert.com.br;
        
        # Redirecionar HTTP para HTTPS
        return 301 https://$server_name$request_uri;
    }

    # Servidor específico para domínio raiz (sem www)
    server {
        listen 80;
        server_name gestaoconsert.com.br;
        
        # Forçar redirecionamento para www
        return 301 https://www.gestaoconsert.com.br$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name www.gestaoconsert.com.br;

        # SSL Configuration com certificados Let's Encrypt
        ssl_certificate /etc/letsencrypt/live/gestaoconsert.com.br/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/gestaoconsert.com.br/privkey.pem;
        
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;

        # Security headers
        add_header X-Frame-Options DENY;
        add_header X-Content-Type-Options nosniff;
        add_header X-XSS-Protection "1; mode=block";

        # Proxy para a aplicação Next.js
        location / {
            proxy_pass http://app;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # Health check
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }

    # Servidor HTTPS para domínio raiz (redireciona para www)
    server {
        listen 443 ssl http2;
        server_name gestaoconsert.com.br;

        # SSL Configuration com certificados Let's Encrypt
        ssl_certificate /etc/letsencrypt/live/gestaoconsert.com.br/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/gestaoconsert.com.br/privkey.pem;
        
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;

        # Forçar redirecionamento para www
        return 301 https://www.gestaoconsert.com.br$request_uri;
    }
}
